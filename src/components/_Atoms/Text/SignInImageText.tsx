import styled from 'styled-components';

const SignInImageText = () => {
  return <Text>로그인</Text>;
};

export default SignInImageText;

const Text = styled.p`
  font-size: 1.2rem;
  font-weight: 400;
  color: #767676;
`;
