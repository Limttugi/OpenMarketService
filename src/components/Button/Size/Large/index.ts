import styled from 'styled-components';
import { ButtonCS } from '..';

export const LargeButtonCS = styled(ButtonCS)`
  height: 6.8rem;
  font-size: 2.4rem;
  font-weight: 700;
  line-height: 3rem;
`;
