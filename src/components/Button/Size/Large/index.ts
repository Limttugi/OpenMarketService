import styled from 'styled-components';
import { ButtonCS_Radius } from 'components/Button';

export const LargeButtonCS = styled(ButtonCS_Radius)`
  height: 6.8rem;
  font-size: 2.4rem;
  font-weight: 700;
  line-height: 3rem;
`;
