import styled from 'styled-components';
import { ButtonCS } from '..';

export const MediumButtonCS = styled(ButtonCS)`
  height: 6rem;
  font-size: 1.8rem;
  font-weight: 700;
  line-height: 2.2rem;
`;
