import styled from 'styled-components';
import { ButtonCS } from '..';

export const SmallButtonCS = styled(ButtonCS)`
  height: 4rem;
  font-size: 1.6rem;
  font-weight: 500;
  line-height: 2rem;
`;
