import styled from 'styled-components';
import { ButtonCS } from '..';

export const TapButtonCS = styled(ButtonCS)`
  position: relative;
  border: none;
  height: 6rem;
  font-size: 1.8rem;
  font-weight: 700;
  line-height: 2.3rem;
  background-color: white;
`;
